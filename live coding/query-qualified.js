const { count } = require("console");
const { title } = require("process");
const { setFlagsFromString } = require("v8");
const { defaultConfiguration } = require("./job-teste");

SELECT Season, count(name-), NULL,
FROM NAME_ITEM from cliente_id;
group by Name null,
order by count cliente_id
////////////////////

//outra query

select Duration, Episode, title
FROM got_episodes_v4
order by Duration
asc limit 3